function Navbar() {
    return (
        <nav>
            <h2>PrePilot</h2>

            <div>
                <a href="#">Home</a>
                <a href="#">Features</a>
                <a href="#">About</a>
                <button>Login</button>
            </div>
        </nav>
    );
}

export default Navbar;